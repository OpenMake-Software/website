apiVersion: v1
kind: Service
metadata:
  name: {{ include "om-website.name" . }}
spec:
  selector:
    app: {{ include "om-website.name" . }}
    tier: backend
  ports:
    - name: hugo
      protocol: TCP
      port: 80
      targetPort: 80
